import{useEffect,useState}from"react";import{FaUserCircle}from"react-icons/fa";import api from"../services/api";import Layout from"../components/Layout";
export default function Profile(){const[u,setU]=useState(null);useEffect(()=>{api.get("/profile/me").then(r=>setU(r.data))},[]);return <Layout><div className="page-head"><div><div className="eyebrow">ACCOUNT</div><h1>Profile</h1><p className="muted">Your BudgetBuddy account.</p></div></div><div className="profile-card">{u?<><FaUserCircle className="avatar"/><h2>{u.name}</h2><p>{u.email}</p><span>{u.role}</span></>:<div className="loading">Loading...</div>}</div></Layout>}
